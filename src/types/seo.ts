export type SEO = {
  pageTitle: string,
  pageDescription: string,
  pageUrl: string,
  pageImage: string
}

export type MEDADATASEO = {
  id: string,
  title: string,
  url: string,
  site_name: string,
  description: string,
  image: {
    url: string,
    height: string,
    width: string
  }
}